#==============================================================================
# ERPNext Multi-Module SaaS Platform - Production Environment
# Docker Compose Configuration
# Version: 1.0.0
#
# Production Environment:
# - Ports: 3010-3019, 3306, 6379, 6383-6384
# - Container names: prod-* prefix
# - Volumes: prod-* prefix
# - Network: production-network
#==============================================================================

version: "3.9"

x-erpnext-common: &erpnext-common
  image: frappe/erpnext:v16
  environment: &erpnext-env
    - FRAPPE_SITE_NAME_HEADER=${SITE_NAME:-erpnext.local}
    - DB_HOST=mariadb
    - REDIS_CACHE=redis-cache:6379
    - REDIS_QUEUE=redis-queue:6379
    - REDIS_SOCKETIO=redis-socketio:6379
    - SOCKETIO_PORT=9000
  volumes:
    - prod-sites-vol:/home/frappe/frappe-bench/sites
    - prod-logs-vol:/home/frappe/frappe-bench/logs
  depends_on:
    mariadb:
      condition: service_healthy
    redis-cache:
      condition: service_started
    redis-queue:
      condition: service_started
    redis-socketio:
      condition: service_started
  networks:
    - production-network

services:
  #============================================================================
  # Database Services
  #============================================================================
  mariadb:
    image: mariadb:10.6
    container_name: prod-mariadb
    restart: unless-stopped
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --skip-innodb-read-only-compressed
      - --innodb-file-format=Barracuda
      - --innodb-file-per-table=1
      - --innodb-large-prefix=1
      - --innodb-buffer-pool-size=2G
      - --max-allowed-packet=256M
      - --max-connections=500
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_NAME:-erpnext}
      MYSQL_USER: ${DB_USER:-erpnext}
      MYSQL_PASSWORD: ${DB_PASSWORD:-erpnextpassword}
    volumes:
      - prod-mariadb-data:/var/lib/mysql
      - ./databases/init:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - production-network

  #============================================================================
  # Redis Services
  #============================================================================
  redis-cache:
    image: redis:7-alpine
    container_name: prod-redis-cache
    restart: unless-stopped
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - prod-redis-cache-data:/data
    ports:
      - "6379:6379"
    networks:
      - production-network

  redis-queue:
    image: redis:7-alpine
    container_name: prod-redis-queue
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - prod-redis-queue-data:/data
    ports:
      - "6383:6379"
    networks:
      - production-network

  redis-socketio:
    image: redis:7-alpine
    container_name: prod-redis-socketio
    restart: unless-stopped
    volumes:
      - prod-redis-socketio-data:/data
    ports:
      - "6384:6379"
    networks:
      - production-network

  #============================================================================
  # ERPNext Core Services
  #============================================================================
  erpnext-backend:
    <<: *erpnext-common
    container_name: prod-erpnext-backend
    restart: unless-stopped
    command: ["bench", "serve", "--port", "8000"]
    ports:
      - "3014:8000"

  erpnext-socketio:
    <<: *erpnext-common
    container_name: prod-erpnext-socketio
    restart: unless-stopped
    command: ["node", "/home/frappe/frappe-bench/apps/frappe/socketio.js"]
    ports:
      - "3016:9000"

  erpnext-scheduler:
    <<: *erpnext-common
    container_name: prod-erpnext-scheduler
    restart: unless-stopped
    command: ["bench", "schedule"]

  erpnext-worker-short:
    <<: *erpnext-common
    container_name: prod-erpnext-worker-short
    restart: unless-stopped
    command: ["bench", "worker", "--queue", "short"]

  erpnext-worker-long:
    <<: *erpnext-common
    container_name: prod-erpnext-worker-long
    restart: unless-stopped
    command: ["bench", "worker", "--queue", "long"]

  erpnext-worker-default:
    <<: *erpnext-common
    container_name: prod-erpnext-worker-default
    restart: unless-stopped
    command: ["bench", "worker", "--queue", "default"]

  #============================================================================
  # ABP SaaS Backend
  #============================================================================
  abp-backend:
    build:
      context: ../abp-backend
      dockerfile: Dockerfile
    container_name: prod-abp-backend
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__Default=Server=mariadb;Port=3306;Database=saas_admin;Uid=root;Pwd=${DB_ROOT_PASSWORD:-rootpassword}
      - App__SelfUrl=https://os.doganconsult.com
      - App__CorsOrigins=https://admin.doganconsult.com,https://tenant.doganconsult.com,https://portal.doganconsult.com
      - AuthServer__Authority=https://os.doganconsult.com
      - AuthServer__RequireHttpsMetadata=false
      - ASPNETCORE_URLS=http://+:5000
      - Abp__Localization__DefaultLanguage=en
    ports:
      - "3015:5000"
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      - production-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3

  #============================================================================
  # SaaS Admin Portal
  #============================================================================
  saas-admin-portal:
    build:
      context: ../saas-admin-portal
      dockerfile: Dockerfile
    container_name: prod-saas-admin-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - API_URL=http://erpnext-backend:8000
      - DATABASE_URL=mysql://root:${DB_ROOT_PASSWORD:-rootpassword}@mariadb:3306/saas_admin
    ports:
      - "3010:3000"
    depends_on:
      - mariadb
      - erpnext-backend
    networks:
      - production-network

  #============================================================================
  # Tenant Admin Portal
  #============================================================================
  tenant-admin-portal:
    build:
      context: ../tenant-admin-portal
      dockerfile: Dockerfile
    container_name: prod-tenant-admin-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - API_URL=http://erpnext-backend:8000
      - DATABASE_URL=mysql://root:${DB_ROOT_PASSWORD:-rootpassword}@mariadb:3306/tenant_admin
    ports:
      - "3011:3000"
    depends_on:
      - mariadb
      - erpnext-backend
    networks:
      - production-network

  #============================================================================
  # Customer Portal
  #============================================================================
  customer-portal:
    build:
      context: ../customer-portal
      dockerfile: Dockerfile
    container_name: prod-customer-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - API_URL=http://erpnext-backend:8000
    ports:
      - "3012:3000"
    depends_on:
      - erpnext-backend
    networks:
      - production-network

  #============================================================================
  # Showcase Pages
  #============================================================================
  showcase-pages:
    build:
      context: ../showcase-pages
      dockerfile: Dockerfile
    container_name: prod-showcase-pages
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "3013:3000"
    networks:
      - production-network

  #============================================================================
  # Nginx Reverse Proxy
  #============================================================================
  nginx:
    image: nginx:1.25-alpine
    container_name: prod-nginx
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - prod-sites-vol:/home/frappe/frappe-bench/sites:ro
      - prod-nginx-logs:/var/log/nginx
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - erpnext-backend
      - erpnext-socketio
      - saas-admin-portal
      - tenant-admin-portal
      - customer-portal
      - showcase-pages
    networks:
      - production-network

#==============================================================================
# Networks
#==============================================================================
networks:
  production-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.30.0.0/16

#==============================================================================
# Volumes
#==============================================================================
volumes:
  prod-mariadb-data:
    driver: local
  prod-redis-cache-data:
    driver: local
  prod-redis-queue-data:
    driver: local
  prod-redis-socketio-data:
    driver: local
  prod-sites-vol:
    driver: local
  prod-logs-vol:
    driver: local
  prod-nginx-logs:
    driver: local
